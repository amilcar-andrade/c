#include <math.h>
#include <stdlib.h>	// for itoa() call
#include <stdio.h>	// for printf() call
#include <string.h>


int main(){ 
char a= {"pepe"};
char b= {"peperrini"};
	int d = 131;
	int da = 13;
	int c=0;

	
	
	int x = -5;
	int as = 5;

	char buffer1[50];
	char buffer0[50];
	char buffer[50];

	
	sprintf( buffer0, "%d", x );
	sprintf( buffer1, "%d", as);
	strcat(buffer0,buffer1);
	
	printf("String obtained on concatenation is %s\n",buffer0);
	
			


c = d==da;
	printf("value %s\n",buffer); 

		printf("value %i\n",c); 


	
	
	
	
  return 0; 
}